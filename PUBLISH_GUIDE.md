# Руководство по публикации сайта hnp.moscow

## ✅ Выполненные исправления

### 1. Обновлена информация о руководителе
- Имя: Хромченков Олег Валерьевич (добавлено отчество)
- Опыт: 20 лет (было 15 лет)

### 2. Исправлены ошибки в ссылках
- `href="#"` заменено на `href="tel:+79262796350"` в кнопках услуг
- Удалена несуществующая ссылка `#about` из footer

### 3. Подготовка к GitHub Pages
- Создан файл `static/CNAME` с доменом hnp.moscow
- Создан workflow `.github/workflows/deploy.yml` для автоматической публикации

## 🚀 Инструкция по публикации

### Шаг 1: Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Назовите репозиторий: `hnp-moscow` (или любое другое имя)
3. Оставьте публичным (Public)
4. Не добавляйте README, .gitignore или лицензию
5. Нажмите "Create repository"

### Шаг 2: Загрузка проекта

Выполните в терминале (в папке проекта):

```bash
# Инициализация git (если ещё не инициализирован)
git init

# Добавление всех файлов
git add .

# Коммит
git commit -m "Initial commit: Hugo site for hnp.moscow"

# Добавление удалённого репозитория (замените USERNAME на ваш логин GitHub)
git remote add origin https://github.com/USERNAME/hnp-moscow.git

# Отправка на GitHub
git push -u origin main
```

### Шаг 3: Настройка GitHub Pages

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Pages**
3. В разделе "Source" выберите **GitHub Actions**
4. Сохраните настройки

### Шаг 4: Настройка DNS для домена hnp.moscow

У вашего регистратора домена добавьте следующие записи:

**Для apex домена (hnp.moscow):**
```
A записи:
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

**Для www (www.hnp.moscow):**
```
CNAME запись:
www → ваш-username.github.io
```

### Шаг 5: Настройка кастомного домена в GitHub

1. В репозитории перейдите в **Settings** → **Pages**
2. В разделе "Custom domain" введите: `hnp.moscow`
3. Нажмите **Save**
4. Дождитесь проверки DNS (может занять до 24 часов)

### Шаг 6: Проверка SSL

GitHub автоматически выпустит SSL-сертификат. После настройки DNS:
1. Убедитесь, что галочка "Enforce HTTPS" включена в настройках Pages
2. Проверьте работу сайта по адресу https://hnp.moscow

## 📋 Структура сайта

```
hnp.moscow/
├── /                    # Главная страница
├── /pravo-sobstvennosti/           # Право собственности (pillar)
│   ├── /kadastrovyj-pasport/      # Кадастровые документы (cluster)
│   ├── /privatizaciya/            # Приватизация (cluster)
│   └── /rosreestr/                # Росреестр (cluster)
└── /zemelnye-spory/                # Земельные споры (pillar)
    ├── /mezhevanie/               # Межевание (cluster)
    ├── /sosedi/                   # Споры с соседями (cluster)
    └── /zaxvat/                   # Самовольный захват (cluster)
```

## ⚠️ Важные замечания

1. **Навигация в footer**: Ссылки `#services`, `#team`, `#cases`, `#contacts` работают только на главной странице. На внутренних страницах они ведут на главную с соответствующим якорем.

2. **Изображения**: Фото руководителя `/images/team/khromchenkov.jpg` отсутствует. Добавьте его в папку `static/images/team/`.

3. **Автоматическая публикация**: После настройки GitHub Actions сайт будет автоматически обновляться при каждом push в ветку main.

## 🆘 Поддержка

При возникновении проблем:
- Проверьте логи сборки в разделе **Actions** репозитория
- Убедитесь, что DNS-записи корректно применились (используйте `dig hnp.moscow`)
- Проверьте, что файл `CNAME` находится в папке `static/`